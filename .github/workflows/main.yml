on:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

jobs:
  lintrunner:
    runs-on: ubuntu-latest
    name: Test the action
    steps:
      - uses: actions/checkout@v3
      - uses: justinchuby/lintrunner-action@4f1caff075db8509d66097059a22deaded6844b5
